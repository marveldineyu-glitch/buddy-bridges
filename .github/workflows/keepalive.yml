name: Keep Alive 🟢

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping buddy-bridges
        run: curl -s https://buddy-bridges.onrender.com
      - name: Ping gpt-mabu205
        run: curl -s https://gpt-mabu205-1.onrender.com
